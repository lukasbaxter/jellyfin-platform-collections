name: "Streaming Collections"
guid: "b2c3d4e5-6f70-4a8b-9c0d-1e2f3a4b5c6d"
version: "1.0.0.0"
targetAbi: "10.11.0.0"
framework: "net8.0"
overview: "Tag media by streaming service and build matching collections."
description: >
  Tags movies and shows by the streaming service they're available on (via TMDB
  watch providers) and builds matching Jellyfin collections. Caches API calls
  on disk. Does not move files.
category: "Metadata"
owner: "lukasbaxter"
artifacts:
  - "Jellyfin.Plugin.StreamingCollections.dll"
